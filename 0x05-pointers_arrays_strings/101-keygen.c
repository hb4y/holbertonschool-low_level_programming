#include "holberton.h"

/**
 * main - main to print a valid pass.
 * Return: 0 on sucess
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand();
	n = (n/n) * 88;
	printf("zzzzzzzzzzzzzzzzzzzzzz");
	printf("%c", n);
	return (0);
}
